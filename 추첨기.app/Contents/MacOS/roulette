#!/bin/bash
# 추첨기 앱 번들 진입점.
#
# 여기서 서버와 크롬을 직접 붙들고 있으면 LaunchServices 가 앱 실행이 끝나기를
# 기다리다 타임아웃(-1712)을 내고 "응답 없음"으로 표시한다. 실제 작업은 분리된
# 프로세스로 넘기고 이 스크립트는 곧바로 끝낸다.

PROJECT="$(cd "$(dirname "$0")/../../.." && pwd)"
nohup "$PROJECT/scripts/launch-roulette.sh" >/dev/null 2>&1 &
exit 0
